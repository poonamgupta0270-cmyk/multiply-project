# Multiply Project

This project contains a simple JavaScript function written using ES6 arrow function syntax.

## 🔢 Function
The function `multiply(a = 1, b = 1)` multiplies two numbers.
It also uses default values.  
If you don’t pass any values, it returns **1**.

## 📌 Example
```javascript
multiply(3, 4); // Output: 12
multiply();     // Output: 1
